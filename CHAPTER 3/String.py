a = "Abhay"

ashort=a[1:4]   # This is used to print specific word from specific index
                # The last index will be excluded
print(ashort)

LENGTH = len(a)   # len print the length of the word
print(LENGTH)

SIngle = a[1]   # This print only single character 
print(SIngle)
